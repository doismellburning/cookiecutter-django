test:
    post:
        - "cd loremipsum && tox"
        - "cd loremipsum && tox -e honcho":
            background: true
        - "sleep 10"
        - "curl --fail http://localhost:31337/ || (status=$? ; cat nohup.out ; exit $status)  # Port hardcoded in tox.ini"
